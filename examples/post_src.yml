# create with `tinkerctl create -f examples/post_src.yml`
version: 1.0
name: test-post
type: Post
spec:
  post:
    title: 'Post With Source File'
    content:
      - type: markdown
        src: post_file.md
